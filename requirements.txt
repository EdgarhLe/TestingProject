# Notes: commented dependencies are packages that will be required in the future but not yet decided on the verisons or which package to use. The commented dependencies are not required for the current version of the project.

# ── Core ML ─────────────────────────────────────────────
--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple
torch==2.12.0
torchvision==0.27.0
torchaudio==2.11.0
transformers==5.13.0
# Requires ffmpeg to be installed on the system.
--index-url https://download.pytorch.org/whl/cu130
--extra-index-url https://pypi.org/simple
torchcodec==0.14.0
accelerate==1.14.0

# ── Memory optimisation (training) ──────────────────────
bitsandbytes==0.49.2

# ── Embeddings / Index ───────────────────────────────────
# faiss-gpu-cuvs==1.13.1 # install via conda: conda install -c rapidsai -c conda-forge rmm
# sentence-transformers>=3.0.0

# ── ASR ──────────────────────────────────────────────────
# openai-whisper>=20231117
qwen-asr==0.0.6
soundfile==0.14.0

# ── OCR ──────────────────────────────────────────────────
# paddlepaddle-gpu            # hoặc paddlepaddle cho CPU
# paddleocr>=2.7.0

# ── BM25 text search ─────────────────────────────────────
# rank-bm25>=0.2.2

# ── API ──────────────────────────────────────────────────
# fastapi>=0.111.0
# uvicorn[standard]>=0.29.0
# pydantic>=2.7.0

# ── Utilities ────────────────────────────────────────────
pyyaml==6.0.2
numpy==2.5.1
opencv-python==5.0.0.93
Pillow==12.3.0
# tqdm>=4.66.0
python-dotenv==1.2.2
pytest==9.1.1
matplotlib>=3.3.0
requests>=2.23.0
scipy>=1.4.1

# ── Panda-70M download (training/data/downloader/prepare_panda70m.py) ────
yt-dlp>=2024.1.0
imageio-ffmpeg>=0.5.0   # bundled, known-working ffmpeg binary — system
                         # ffmpeg installs have proven unreliable on Windows
